#!/usr/bin/env ruby

class PdfFormatter
  def initialize(#brush options and stuff?)
  end

  def render(h)
    h.keys.each do |point|
        # draw to point.x, point.y -- until curves anyway
    end

    # draw to h[h.keys.first].(x,y) to close figure
  end
end